function setup() {
    createCanvas(windowWidth, windowHeight)

}

function draw() {

}

function windowResized() {
    resizeCanvas(windowWidth, windowHeight)
}

function createEnvironment() {
    const spacing = 80;
    const numTracks = floor(height/80);
    
    for(let i = numTracks; i > 0; i--)
 }